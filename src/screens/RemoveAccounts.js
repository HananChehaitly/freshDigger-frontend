import React, { useEffect, useState } from 'react';
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  StatusBar,
  Keyboard,
  TouchableWithoutFeedback,
  TouchableOpacity,
} from 'react-native';


export default function RemoveAccounts({navigation}) {


    return(
     <Text>This where Admin removes accounts</Text>
    );
}